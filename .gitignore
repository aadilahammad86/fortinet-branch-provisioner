# Secrets
.env

# Config backups may contain sensitive data
configs/*.conf
configs/*.bak

# Cookie / session files
*cookies*.txt

# Python
__pycache__/
*.pyc

# PyInstaller build output (rebuild with: python build_exe.py)
build/
dist/
*.spec

# Saved branch templates name real sites and subnets -- share them with the
# GUI's Export…/Import… buttons, not through the repo.
branches/
profiles/

# OS
Thumbs.db
Desktop.ini
